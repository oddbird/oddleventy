{% import 'embed.macros.njk' as embed %}

{% if image.src %}
  <div id="hero">
    {% set style = {'--hero-position', image.position} | styles %}
    {{ embed.img(
      src=image.src,
      alt=image.alt,
      attrs={'style': style}
    ) }}
  </div>
{% endif %}
