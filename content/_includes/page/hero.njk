{% import 'embed.macros.njk' as embed %}

{% if image.src %}
  <div id="hero">
    {% set style = ['--hero-position', image.position] | join(':') if image.position else none %}
    {{ embed.img(
      src=image.src,
      alt=image.alt,
      attrs={'style': style}
    ) }}
  </div>
{% endif %}
