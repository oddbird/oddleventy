{%- set tag = related_tag -%}

{% if tag %}
  <section data-list="tag" data-typeset>
    {{ post.related_posts(
      tag,
      collections,
      page_url=page.url
    ) }}
  </section>
{% endif %}
